#ifndef OBJECT_EXTENSION_H
#define OBJECT_EXTENSION_H

class ObjectExtension {
public:
  // Putting this here is kind of awkward, but for the moment I like it better than the alternatives
  enum Type {
    Composition,
    LAST
  };

public:

private:
};

#endif
